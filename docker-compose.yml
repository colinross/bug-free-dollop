version: '2'
services:
  web:
    build:
      context: .
      dockerfile: web.Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql://postgres:password@db/
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=password
